# Cross-service tests

Root-level integration/E2E tests that span multiple services (Playwright browser
flows, full-stack smoke tests) go here. Per-service unit tests live alongside each
service instead (`services/api/tests`, `apps/web/src/**/*.test.ts`).

Empty for now — Phase 0 only needs per-service unit/smoke tests.
